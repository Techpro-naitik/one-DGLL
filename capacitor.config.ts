import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.dgll.com',
  appName: 'one-DGLL',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
